fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0
# pyModeS 3.x is a breaking rewrite (removes the pms.df/pms.icao/pms.adsb.*
# functional API that app/decoder.py depends on) — pin below 3.0.
# decoder.py also uses pms.adsb.surface_velocity (TC 5-8 ground track),
# present throughout the 2.x line (verified on 2.22.0).
pyModeS>=2.21,<3.0
httpx>=0.27.0
